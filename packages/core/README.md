# @skyline-ts/core

The skyline-ts runtime — unified messaging for TypeScript agents.

```bash
bun add @skyline-ts/core
```

Pair with platform packages (`@skyline-ts/imessage`, `@skyline-ts/slack`, …) or install the batteries-included `skyline-ts` umbrella.
